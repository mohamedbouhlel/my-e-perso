services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: my-e-perso-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:8090:80"
    networks:
      - default
      - compose_edge

networks:
  default:
  compose_edge:
    external: true
